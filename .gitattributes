# Set default behavior to automatically normalize line endings
* text=auto

# Rust files should always use LF
*.rs text eol=lf

# Cargo files should always use LF
Cargo.toml text eol=lf
Cargo.lock text eol=lf

# JSON files should always use LF
*.json text eol=lf

# Markdown files should always use LF
*.md text eol=lf

# YAML files should always use LF
*.yml text eol=lf
*.yaml text eol=lf

# Shell scripts should always use LF
*.sh text eol=lf

# Docker files should always use LF
Dockerfile text eol=lf
.dockerignore text eol=lf

# Git files should always use LF
.gitignore text eol=lf
.gitattributes text eol=lf

# Makefile should always use LF
Makefile text eol=lf

# PowerShell scripts can use CRLF
*.ps1 text eol=crlf
